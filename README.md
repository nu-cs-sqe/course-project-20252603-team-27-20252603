![Gradle Build](https://github.com/nu-cs-sqe/course-project-20252603-team-27-20252603/actions/workflows/main.yml/badge.svg)

[![Open in Codespaces](https://classroom.github.com/assets/launch-codespace-2972f46106e565e64193e422d61a12cf1da4916b45550586e14ef0a7c637dd04.svg)](https://classroom.github.com/open-in-codespaces?assignment_repo_id=23625230)
# PROJECT NAME

## Contributors
- Olivia Mofus
- Jerome Bizimana
- Muye Chen
- Rishi Ramaiya

## Dependencies
- JDK 11
- JUnit 5.10
- Gradle 8.10
- EasyMock 5.2.0
- SpotBugs Annotations 4.8.6

## Acknowledgements
- GUI example code given by Professor Yiji Zhang
- Support and feedback provided by the TAs and Peer Mentors of CS 380 (Spring '26)

## Test and Code Quality
### Tools
- JUnit 5.10
- Gradle 8.10
- SpotBugs 6.0.25
- Pitest 1.19.0
- Jacoco 0.8.14
- Checkstyle 10.12.5

### Test Coverage (Non-UI Code)
- 100% Line Coverage
- 100% Cyclomatic Coverage

### Mutation Testing Analysis and Equivalent Mutants
#### Game.java:
1) Castling Side Determination (Conditional Boundary Mutant)
   - Target Line: boolean kingSide = to.getCol() > from.getCol();
   - Mutation Type: Changed conditional Boundary
   - Justification of Equivalence: This line can't be killed since if to.getCol() = from.getCol(), the methods will return false before this line. This line is used to determine which direction the king goes

#### Piece.java:
1) Rook Movement Directionality Check (Arithmetic Mutant) 
   - Target Line: int colDiff = to.getCol() - from.getCol(); inside isValidRookMove(...)
   - Mutation Type: Arithmetic Substitution 
   - Justification of Equivalence: The value calculated in colDiff is consumed immediately below it exclusively within a boolean evaluation: `boolean isStraightLine = (rowDiff != 0 && colDiff == 0) || (colDiff != 0 && rowDiff == 0);` This conditional logic exclusively evaluates whether colDiff is exactly zero or non-zero, completely discarding the mathematical sign and the step's exact magnitude. Consequently, if a mutation flips the subtraction order and changes a value like 4 to -4, the check still evaluates -4 != 0 as true and routes the logic identically. Because any non-zero value satisfies the condition exactly like the original calculation, the program behavior remains untouched, rendering the mutant completely equivalent.
2) Diagonal Path Obstruction Indexing (Arithmetic Mutant)
   - Target Line: new Location(from.getRow() + i * rowStep, from.getCol() + i * colStep)
   - Mutation Type: Arithmetic Substitution
   - Justification of Equivalence: This loop scans the intermediate squares of a diagonal trajectory to check for blocking pieces. Because rowStep and colStep are generated by Integer.compare(), their values are restricted entirely to a baseline of 1 or -1 during any true diagonal movement. Mathematically, multiplying any loop index i by 1 or -1 yields an identical numerical value to adding or subtracting i directly. Because the resulting coordinate lookups remain entirely unchanged across all valid diagonal vectors, the branch execution matches the original code exactly, proving the mutant is equivalent.

#### Total Mutation Coverage 
- Line coverage: 100%
- Mutation coverage: 99%, 469/473
- Test strength: 99%, 469/473

### Checkstyle
- 100% pass for the checkstyle

### SpotBugs
- SpotBugs reports no issues detected in this repository

## Special Decisions, Exceptions, and New Implementations
- Omitting Mocks for Lightweight Value Objects (Player and Location):
  - We chose not to mock the Player and Location classes. Both serve strictly as objects and value holders, containing basic state data accessed via simple getters and setters. When attempting to mock these classes (specifically Player in GameTest), we could not because the classes were `public final`. When we tried to remove the `final`, this caused other errors that cascaded throuhout the repository. After doing research online which said that value-holder classes like Player and Location are not dependencies that need to be mocked, we decided to not mock these classes in tests. 
- Implementation of Blitz Shot-Clocks (1-Minute Timed Turns):
  - As a custom feature extension to standard gameplay rules, we integrated an active countdown timer that restricts each player to a 1-minute time limit per turn.